{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "allowJs": true,
        "resolveJsonModule": true,
        "baseUrl": "./",
        "paths": {
            "@websmith/cli": ["../cli/src"]
        } /* A series of entries which re-map imports to lookup locations relative to the 'baseUrl'. */
    },
    "include": ["src/**/*.ts"]
}
