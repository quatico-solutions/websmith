{
    "targets": {
        "client": {
            "writeFile": true,
            "addons": ["client-function-transform", "export-collector", "foobar-replace-emitter"],
            "config": { "functionsDir": "./src", "debug": true },
            "options": { "outDir": "./lib/client" }
        },
        "server": {
            "writeFile": true,
            "addons": ["service-function-generate"],
            "config": { "functionsDir": "./src", "debug": true},
            "options": { "outDir": "./lib/server" } 
        }
    }
}
