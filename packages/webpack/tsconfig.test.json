{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "baseUrl": "./" /* Base directory to resolve non-absolute module names. */,
        "paths": {
            "@websmith/cli": ["../cli/src"],
            "@websmith/compiler": ["../compiler/src"]
        } 
    },

    "include": ["src/**/*.ts", "test/**/*.ts"]
}
